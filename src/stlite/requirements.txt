# Stlite Dependencies (Browser Runtime via Pyodide)
# Use no version specifiers - let Pyodide pick compatible versions

pandas
numpy
scipy
altair

# Note: streamlit is provided by stlite itself
# Note: polars is NOT compatible with Pyodide